import { EquipmentTypePipe } from './equipment-type.pipe';

describe('EquipmentTypePipe', () => {
  it('create an instance', () => {
    const pipe = new EquipmentTypePipe();
    expect(pipe).toBeTruthy();
  });
});
